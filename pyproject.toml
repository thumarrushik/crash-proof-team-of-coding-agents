[project]
name = "temporal-claude-code"
version = "0.1.0"
description = "Headless Claude Code as Temporal activities — a durable agent loop"
requires-python = ">=3.11"
dependencies = [
    "temporalio>=1.30",
    "claude-agent-sdk>=0.2.116",
]

[tool.uv]
package = false
