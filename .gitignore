.venv/
__pycache__/
*.pyc
.DS_Store
*.log
.env
experiment-results*/

# experiment run logs (raw; curated results live in deploy/*-results.md)
deploy/*.log

# Claude Code local state at the REPO ROOT only (worktrees, session scratch).
# Anchored on purpose: teams/*/.claude is the committed governance unit —
# an unanchored .claude/ silently excluded every team's policy and skills.
/.claude/

# live dev-server state (file-backed history; never committed)
deploy/temporal-dev.db
